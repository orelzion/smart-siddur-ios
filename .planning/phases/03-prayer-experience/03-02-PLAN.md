---
phase: 03-prayer-experience
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Sources/Core/Models/Domain/CachedPrayer.swift
  - Sources/Services/PrayerCacheService.swift
  - Sources/Features/Prayers/PrayerTextViewModel.swift
  - Sources/Features/Prayers/PrayersMenuViewModel.swift
  - Sources/App/SmartSiddurApp.swift
  - Sources/Core/DI/DependencyContainer.swift
autonomous: true
must_haves:
  truths:
    - "Prayers are pre-fetched for 14 days and cached in SwiftData"
    - "Cached prayers display correctly in airplane mode with no network"
    - "Changing settings (nusach, location) invalidates the cache and re-fetches prayers"
  artifacts:
    - path: "Sources/Core/Models/Domain/CachedPrayer.swift"
      provides: "SwiftData model for offline prayer storage"
      contains: "@Model CachedPrayer"
    - path: "Sources/Services/PrayerCacheService.swift"
      provides: "14-day pre-fetch and cache management"
      exports: ["prefetchPrayers", "getCachedPrayer", "invalidateCache"]
    - path: "Sources/Features/Prayers/PrayerTextViewModel.swift"
      provides: "Offline-aware prayer loading with fallback"
      min_lines: 60
  key_links:
    - from: "Sources/Services/PrayerCacheService.swift"
      to: "supabase/functions/generate-prayer-batch"
      via: "batch pre-fetch API calls"
      pattern: "generatePrayerBatch"
    - from: "Sources/Features/Prayers/PrayerTextViewModel.swift"
      to: "Sources/Services/PrayerCacheService.swift"
      via: "cache lookup before network"
      pattern: "cacheService\.getCachedPrayer"
    - from: "Sources/App/SmartSiddurApp.swift"
      to: "Sources/Services/PrayerCacheService.swift"
      via: "background refresh on app launch"
      pattern: "cacheService\.prefetchPrayers"
---

<objective>
Implement 14-day offline prayer cache with SwiftData, background pre-fetching, and automatic cache invalidation when settings change.

Purpose: Enable seamless offline prayer access and reduce network usage through intelligent caching
Output: Offline-first prayer experience with automatic cache management
</objective>

<execution_context>
@/Users/orelzion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/orelzion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3 Context
@.planning/phases/03-prayer-experience/03-CONTEXT.md

# Prerequisites
@.planning/phases/03-prayer-experience/03-01-PLAN.md
@.planning/phases/02-ios-core/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create SwiftData cache model and service</name>
  <files>Sources/Core/Models/Domain/CachedPrayer.swift, Sources/Services/PrayerCacheService.swift</files>
  <action>
    1. Create CachedPrayer.swift with:
       - @Model CachedPrayer class with SwiftData persistence
       - id: UUID primary key
       - prayerType: String (PrayerType.rawValue)
       - date: Date (cached prayer date)
       - nusach: String (nusach identifier)
       - locationId: UUID (location identifier for geo-specific prayers)
       - content: String (full rendered prayer text)
       - settingsHash: String (hash of relevant settings for invalidation)
       - contentVersion: Int (backend content version for updates)
       - cachedAt: Date (when cached)
       - expiresAt: Date (cache expiration)

    2. Create PrayerCacheService.swift that:
       - Manages SwiftData context and CachedPrayer CRUD
       - Implements prefetchPrayers(from:to:settings:) async to fetch 14 days
       - Implements getCachedPrayer(type:date:settings:) async with fallback to network
       - Implements invalidateCache() to clear all cached prayers
       - Generates settingsHash from nusach, location, tfilaMode for invalidation
       - Implements background refresh when cache expires
       - Handles memory vs disk cache decisions
       - Uses generate-prayer-batch edge function for efficient pre-fetch

    3. Cache strategy:
       - Pre-fetch all essential prayers for 14-day window
       - Cache key: prayerType + date + settingsHash + contentVersion
       - Expiration: 7 days for most prayers, 1 day for calendar-sensitive ones
       - Background refresh: runs when app launches and device is online
       - Silent updates: no UI disruption during cache refresh
  </action>
  <verify>Build succeeds - SwiftData model compiles with proper attributes</verify>
  <done>Cache layer created with SwiftData model and management service</done>
</task>

<task type="auto">
  <name>Update prayer view models to use cache</name>
  <files>Sources/Features/Prayers/PrayerTextViewModel.swift, Sources/Features/Prayers/PrayersMenuViewModel.swift</files>
  <action>
    1. Update PrayerTextViewModel.swift to:
       - Add prayerCacheService: PrayerCacheService via DependencyContainer
       - Modify loadPrayer() to check cache first, then network as fallback
       - Show loading state during network fetch, cached content during offline
       - Save successful network responses to cache
       - Handle cache expiration gracefully
       - Provide offline indication (subtle, not blocking per user decisions)

    2. Update PrayersMenuViewModel.swift to:
       - Add prayerCacheService: PrayerCacheService via DependencyContainer  
       - Modify loadTodaysPrayers() to use cache when available
       - Trigger background prefetch for upcoming prayers
       - Invalidate cache when relevant settings change
       - Ensure prayer availability in offline mode

    3. Cache-first approach:
       - Always try cache first for instant response
       - Network fetch only if cache miss or expired
       - Seamless transition from cached to fresh content
       - No blocking "offline" indicators
       - Graceful degradation when cache expires
  </action>
  <verify>Build succeeds - view models integrate with cache service</verify>
  <done>Prayer views now use cache-first loading with network fallback</done>
</task>

<task type="auto">
  <name>Implement cache invalidation and background refresh</name>
  <files>Sources/App/SmartSiddurApp.swift, Sources/Core/DI/DependencyContainer.swift, Sources/Features/Settings/SettingsViewModel.swift</files>
  <action>
    1. Update SmartSiddurApp.swift to:
       - Add .modelContainer(for: CachedPrayer.self) to main app
       - Initialize PrayerCacheService in DependencyContainer
       - Trigger background cache refresh on app launch
       - Handle app backgrounding/foregrounding for cache management

    2. Update DependencyContainer.swift to:
       - Add prayerCacheService: PrayerCacheService
       - Initialize with SwiftData context
       - Expose to prayer views and settings

    3. Update SettingsViewModel.swift to:
       - Import PrayerCacheService via DependencyContainer
       - Invalidate cache when nusach changes
       - Invalidate cache when location changes  
       - Invalidate cache when tfilaMode changes
       - Trigger cache refresh after settings update
       - Use settingsHash to detect relevant changes only

    4. Invalidation strategy:
       - Clear entire cache when relevant settings change
       - Preserve cache for unrelated setting changes (theme, notifications)
       - Automatic background refresh after invalidation
       - Settings change detection via hash comparison
  </action>
  <verify>Build succeeds - app initializes SwiftData and cache properly</verify>
  <done>Cache invalidation implemented on settings changes with auto-refresh</done>
</task>

<task type="auto">
  <name>Implement 14-day pre-fetch and content versioning</name>
  <files>Sources/Services/PrayerCacheService.swift</files>
  <action>
    1. Enhance PrayerCacheService.swift with:
       - prefetchPrayers() method that calls generate-prayer-batch for 14-day window
       - Get current content version from backend to detect updates
       - Selective pre-fetch: essential prayers first, then comprehensive
       - Rate limiting: prevent excessive API calls during background refresh
       - Progress tracking for prefetch operations (optional, not shown in UI)

    2. Content versioning:
       - Check global content version on each refresh
       - Invalidate cache if backend content version changes
       - Preserve user-specific customizations if any (future enhancement)
       - Handle content updates transparently

    3. Performance optimization:
       - Batch API calls to minimize network requests
       - Pre-fetch during idle time or on WiFi only (configurable)
       - Intelligent scheduling based on user prayer usage patterns
       - Cache size management: limit storage usage

    4. Background refresh timing:
       - On app launch (if cache > 12 hours old)
       - When device comes online after being offline
       - After significant settings changes
       - Periodic refresh every 24 hours
  </action>
  <verify>Build succeeds - pre-fetch logic compiles without errors</verify>
  <done>14-day pre-fetch implemented with intelligent background refresh</done>
</task>

<task type="auto">
  <name>Add SwiftData configuration and cleanup</name>
  <files>Sources/App/SmartSiddurApp.swift, Sources/Core/Models/Domain/CachedPrayer.swift</files>
  <action>
    1. Update SmartSiddurApp.swift to:
       - Configure SwiftData model container with proper store description
       - Add migration handling for future cache model changes
       - Set up automatic scheduled migration
       - Configure store location and attributes

    2. Enhance CachedPrayer.swift with:
       - Proper SwiftData indexes for efficient queries
       - Unique constraints on prayerType+date+settingsHash
       - Delete rules for cascade cleanup
       - Query optimization for cache lookups

    3. Cache cleanup:
       - Remove expired entries automatically
       - Limit cache size to reasonable storage limits
       - Cleanup orphaned entries after settings changes
       - Background maintenance tasks

    4. Error handling:
       - Graceful SwiftData corruption handling
       - Cache rebuild on migration failures
       - Network timeout handling during prefetch
       - Storage space management
  </action>
  <verify>Build succeeds - SwiftData configuration compiles and runs</verify>
  <done>SwiftData cache properly configured with cleanup and migration</done>
</task>

</tasks>

<verification>
- 14-day prayer cache created in SwiftData with proper indexing
- Pre-fetch functionality loads prayers for upcoming days
- Cache-first loading works with network fallback
- Cache invalidates when nusach, location, or tfilaMode settings change
- Background refresh runs silently when app launches
- Airplane mode testing shows cached prayers display correctly
- Content versioning detects and handles backend updates
- No blocking offline indicators or errors
</verification>

<success_criteria>
Prayers display instantly from cache, refresh automatically when needed, and remain accessible in airplane mode. Settings changes trigger cache updates, and the system efficiently manages storage and network usage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-prayer-experience/03-prayer-experience-02-SUMMARY.md`
</output>