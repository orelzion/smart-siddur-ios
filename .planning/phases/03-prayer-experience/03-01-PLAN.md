---
phase: 03-prayer-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - Sources/Features/Home/TabContainerView.swift
  - Sources/Core/DI/DependencyContainer.swift
  - Sources/Features/Prayers/PrayersMenuView.swift
  - Sources/Features/Prayers/PrayersMenuViewModel.swift
  - Sources/Features/Prayers/PrayerTextView.swift
  - Sources/Features/Prayers/PrayerTextViewModel.swift
  - Sources/Core/Models/Domain/Prayer.swift
  - Sources/Core/Models/Domain/PrayerText.swift
  - Sources/Services/PrayerService.swift
autonomous: false
must_haves:
  truths:
    - "User can see a prayer menu with all 26+ prayer types organized by time of day"
    - "User can open a prayer and see fully rendered text with correct nusach and calendar-sensitive insertions"
  artifacts:
    - path: "Sources/Features/Home/TabContainerView.swift"
      provides: "Navigation container with 4 tabs including Prayers"
      contains: "Prayers tab with NavigationStack"
    - path: "Sources/Features/Prayers/PrayersMenuView.swift"
      provides: "Prayer list organized by time of day with Today section"
      min_lines: 100
    - path: "Sources/Features/Prayers/PrayerTextView.swift"
      provides: "Full scrolling prayer text view with Hebrew and nikud"
      min_lines: 80
    - path: "Sources/Services/PrayerService.swift"
      provides: "API integration for prayer generation"
      exports: ["generatePrayer", "generatePrayerBatch"]
    - path: "Sources/Core/Models/Domain/Prayer.swift"
      provides: "Prayer type definitions and categorization"
      contains: "PrayerType enum with 26+ cases"
  key_links:
    - from: "Sources/Features/Prayers/PrayersMenuView.swift"
      to: "Sources/Services/PrayerService.swift"
      via: "generatePrayer method calls"
      pattern: "prayerService\.generatePrayer"
    - from: "Sources/Features/Prayers/PrayerTextView.swift"
      to: "Sources/Features/Prayers/PrayerTextViewModel.swift"
      via: "@Observable binding"
      pattern: "@Observable.*PrayerTextViewModel"
    - from: "Sources/Services/PrayerService.swift"
      to: "supabase/functions/generate-prayer"
      via: "Supabase client fetch"
      pattern: "supabase\.functions\.invoke"
---

<objective>
Build the prayer menu, text display, and backend integration to allow users to browse and view any prayer with correct nusach and calendar-sensitive insertions.

Purpose: Create the core prayer browsing experience with proper organization and rendering
Output: Working prayer menu and text display connected to backend edge functions
</objective>

<execution_context>
@/Users/orelzion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/orelzion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3 Context
@.planning/phases/03-prayer-experience/03-CONTEXT.md

# Prior Implementation Patterns
@.planning/phases/02-ios-core/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create prayer models and PrayerService</name>
  <files>Sources/Core/Models/Domain/Prayer.swift, Sources/Core/Models/Domain/PrayerText.swift, Sources/Services/PrayerService.swift</files>
  <action>
    1. Create Prayer.swift with:
       - PrayerType enum with all 26+ prayer types (shacharit, mincha, arvit, etc.)
       - PrayerCategory enum (morning, afternoon, evening, special)
       - Prayer struct with id, type, category, displayName, hebrewName, description
       - PrayerRequest struct with type, date, nusach, location, tfilaMode
       - PrayerResponse matching edge function response

    2. Create PrayerText.swift with:
       - PrayerText struct for rendered content with sections
       - PrayerSection struct with title and content
       - Text formatting metadata for nikud/teamim

    3. Create PrayerService.swift that:
       - Wraps Supabase client to call generate-prayer and generate-prayer-batch edge functions
       - Implements generatePrayer(type:date:nusach:location:tfilaMode:) async
       - Implements generatePrayerBatch(from:to:nusach:location:tfilaMode:) async  
       - Handles request/response mapping and error handling
       - Follows same pattern as LocationRepository with proper error types
  </action>
  <verify>Build succeeds - all models compile cleanly</verify>
  <done>Prayer models and service layer created and ready for use</done>
</task>

<task type="auto">
  <name>Build prayer menu screen with organization</name>
  <files>Sources/Features/Prayers/PrayersMenuView.swift, Sources/Features/Prayers/PrayersMenuViewModel.swift</files>
  <action>
    1. Create PrayersMenuViewModel.swift that:
       - Extends @Observable with prayer list state
       - Loads today's relevant prayers using JewishCalendarService
       - Organizes all prayers by category (morning/afternoon/evening/special)
       - Implements getTodaysPrayers() for "Today" section
       - Uses calendar state to filter special occasion prayers
       - Follows same pattern as ZmanimViewModel with async loading

    2. Create PrayersMenuView.swift that:
       - Shows "Today's Prayers" section at top with current day's relevant prayers
       - Organizes remaining prayers by time of day sections
       - Within each section: orders by importance/sequence (main service first)
       - Shows "Special Occasions" section separately for holiday-specific prayers
       - Uses NavigationLink to navigate to prayer text view
       - Follows visual hierarchy similar to CalendarView with sectioned lists
       - Hebrew names with English descriptions (per user decisions)

    3. Prayer menu organization:
       - Morning: Shacharit, Psukei Dezimra, Birchot HaShachar, etc.
       - Afternoon: Mincha, Mincha Gedola, etc.
       - Evening: Arvit, Kriat Shema Al HaMitah, etc.
       - Special: Hallel, Musaf, Selichot (shown only when relevant)
  </action>
  <verify>Build succeeds - menu loads with all prayer sections</verify>
  <done>Prayer menu displays with Today section and proper categorization</done>
</task>

<task type="auto">
  <name>Build prayer text display screen</name>
  <files>Sources/Features/Prayers/PrayerTextView.swift, Sources/Features/Prayers/PrayerTextViewModel.swift</files>
  <action>
    1. Create PrayerTextViewModel.swift that:
       - Extends @Observable with prayer text state
       - Loads prayer using PrayerService.generatePrayer()
       - Caches loaded prayer in memory for navigation back/forth
       - Handles loading and error states
       - Uses DependencyContainer for nusach/location settings

    2. Create PrayerTextView.swift that:
       - Displays full scrolling prayer text (not paginated)
       - Always shows nikud and teamim (vowel points and cantillation marks)
       - Hebrew only text (no translations per user decisions)
       - For repetitions: shows full text first time, "repeat" notation thereafter
       - Uses iOS Dynamic Type for font size controls
       - Table of contents overlay for navigation within long prayers
       - Back button returns to prayer menu (no favorites or recent lists)

    3. Text rendering approach:
       - Use Text view with proper Hebrew font selection
       - Preserve original formatting and line breaks
       - Handle right-to-left text direction
       - Support pinch-to-zoom for accessibility
  </action>
  <verify>Build succeeds - prayer text renders correctly with Hebrew and nikud</verify>
  <done>Prayer text view displays complete prayers with proper formatting</done>
</task>

<task type="auto">
  <name>Add Prayers tab to TabContainerView and wire dependencies</name>
  <files>Sources/Features/Home/TabContainerView.swift, Sources/Core/DI/DependencyContainer.swift</files>
  <action>
    1. Update DependencyContainer.swift to:
       - Add prayerService: PrayerService
       - Initialize prayerService in init() with Supabase client
       - Expose prayerService for prayer views

    2. Update TabContainerView.swift to:
       - Add 4th tab: "Prayers" with book icon
       - Tag prayers tab as index 3 (after Zmanim, Calendar, Settings)
       - Reorder tabs if needed to maintain logical flow
       - Keep existing location setup flow on app launch

    3. Tab structure:
       - Tab 1: Zmanim (index 0)
       - Tab 2: Calendar (index 1) 
       - Tab 3: Prayers (index 2) - new
       - Tab 4: Settings (index 3)
  </action>
  <verify>Build succeeds - Prayers tab appears in navigation</verify>
  <done>Prayers tab added to app navigation with dependency injection</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify prayer browsing experience</name>
  <files>Sources/Features/Home/TabContainerView.swift, Sources/Features/Prayers/PrayersMenuView.swift, Sources/Features/Prayers/PrayerTextView.swift</files>
  <action>Verify the complete prayer browsing experience works correctly</action>
  <what-built>Complete prayer browsing experience with menu, text display, and Prayers tab</what-built>
  <how-to-verify>
    1. Open app and verify Prayers tab appears in tab bar
    2. Tap Prayers tab - verify menu loads with:
       - "Today's Prayers" section at top
       - Morning, Afternoon, Evening, Special Occasions sections
       - All 26+ prayer types listed with Hebrew names
    3. Tap any prayer (e.g., Shacharit) - verify:
       - Prayer text view opens with full scrolling content
       - Hebrew text displays with nikud and teamim
       - Text is readable with proper RTL formatting
       - Back button returns to prayer menu
    4. Test special prayers - verify they only appear when relevant (e.g., Hallel on Rosh Chodesh)
    5. Verify text variations based on current settings (nusach/location)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
  <verify>Manual verification confirms all prayer browsing features work correctly</verify>
  <done>Prayer browsing experience works correctly with all features functional</done>
</task>

</tasks>

<verification>
- Prayer menu loads and displays all 26+ prayer types organized by time of day
- "Today's Prayers" section shows relevant prayers for current date
- Prayer text view opens and displays Hebrew content with nikud and teamim
- Navigation flows correctly between menu and text views
- Prayers tab appears in TabContainerView with proper navigation structure
- All prayers load from backend edge functions with correct nusach and date parameters
</verification>

<success_criteria>
Users can browse and view any prayer with the correct text for today's date, nusach, and settings. The prayer menu is organized intuitively by time of day with a "Today" section, and prayer text displays with proper Hebrew formatting including nikud and teamim.
</success_criteria>

<output>
After completion, create `.planning/phases/03-prayer-experience/03-prayer-experience-01-SUMMARY.md`
</output>